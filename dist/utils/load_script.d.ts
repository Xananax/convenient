/**
 * loads an external script. Useful for loading google maps or other external APIs
 * @param url
 * @param bypassCache
 */
export declare const load_script: (url: string, bypassCache?: boolean) => Promise<{}>;
