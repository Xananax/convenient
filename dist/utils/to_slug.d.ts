/**
 * Converts a string to a safe string,
 * suitable to be used as an url
 * or as an html element class/id
 * @param str
 */
export declare const to_slug: (str: string) => string;
export default to_slug;
